export default class User {

    constructor(fname, lname, bday, login, password) {
        this.fname = fname;
        this.lname = lname;
        this.bday = bday;
        this.login = login;
        this.password = password;
    }
}